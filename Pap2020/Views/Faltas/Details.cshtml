@model Pap2020.Models.Falta

@{
    ViewBag.Title = "Details";
}

<h2>Detalhes da Falta</h2>

<div>
    <h4>Falta</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Data)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Data)
        </dd>

        


        <dt>
            @Html.DisplayNameFor(model => model.Relatorio.nome_empresa)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Relatorio.nome_empresa)
        </dd>

    </dl>
</div>
<p>
    @if (Convert.ToInt32(Session["Tipo"]) == 1)
    {
        @Html.ActionLink("Editar", "Edit", new { id = Model.id_falta })
        <br />
        @Html.ActionLink("Voltar atrás", "Index")
    }
    @if (Convert.ToInt32(Session["Tipo"]) == 2)
    {
        @Html.ActionLink("Editar", "Edit", new { id = Model.id_falta })
        <br />
        @Html.ActionLink("Voltar atrás", "Index")

    }

    @if (Convert.ToInt32(Session["Tipo"]) == 3)
    {
        @Html.ActionLink("Voltar atrás", "Index")
    }
</p>
